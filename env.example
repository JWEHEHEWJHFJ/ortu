# Isi lewat Vercel Dashboard -> Settings -> Environment Variables, JANGAN commit file .env asli.

# Otomatis terisi kalau kamu pakai Vercel KV dari marketplace/dashboard:
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Generate secret acak minimal 32 karakter, misal: openssl rand -base64 32
SESSION_SECRET=
